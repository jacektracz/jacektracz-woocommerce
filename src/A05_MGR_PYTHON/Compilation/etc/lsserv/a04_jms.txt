#!/bin/bash

#source /jenkins/jt_dev/tools/python/A05_MGR_PYTHON/Compilation/etc/setenv/setenv_cmr_dev.txt

PIDSERV=$(ps -ef | grep 'activemq'  |grep 'Dactivemq.home' | grep -v 'grep'| awk '{print $2}')

if [[ -z "$PIDSERV" ]]; then
	echo "JMS_PID::NULL"
else
	echo "JMS_PID:$PIDSERV"
fi	

