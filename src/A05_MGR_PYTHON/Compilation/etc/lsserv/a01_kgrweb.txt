#!/bin/bash

#source /jenkins/jt_dev/tools/python/A05_MGR_PYTHON/Compilation/etc/setenv/setenv_cmr_dev.txt

PIDSERV=$(ps -ef | grep 'web' | grep kgrserver | grep 'kgr.application.server.name=kgrserver' | grep -v 'grep'|  grep -v 'ratemanager'| grep -v 'rtm_ear'| awk '{print $2}')

if [[ -z "$PIDSERV" ]]; then
	echo "KGRWEB_PID_NULL"
else
	echo "KGRWEB_PID:$PIDSERV"
fi


